🔹 Step 1: Update Ubuntu System
sudo apt update
sudo apt upgrade -y

🔹 Step 2: Install Java (Required for Jenkins)
sudo apt install openjdk-17-jdk -y


Verify Java:

java -version


✅ Java version should be displayed


🔹 Step 3: Add Jenkins Repository & Key
curl -fsSL https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key | sudo tee \
/usr/share/keyrings/jenkins-keyring.asc > /dev/null

echo deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
/etc/apt/sources.list.d/jenkins.list > /dev/null


🔹 Step 4: Install Jenkins
sudo apt update
sudo apt install jenkins -y


🔹 Step 5: Start & Enable Jenkins Service
sudo systemctl start jenkins
sudo systemctl enable jenkins


Check status:

sudo systemctl status jenkins


✅ Status should show active (running)


🔹 Step 6: Open Jenkins Dashboard

Open browser and go to:

http://localhost:8080


OR (VM/Server):

http://<server-ip>:8080


🔹 Step 7: Unlock Jenkins

Get initial admin password:

sudo cat /var/lib/jenkins/secrets/initialAdminPassword


➡ Paste password in browser
➡ Click Continue
➡ Select Install Suggested Plugins
➡ Create Admin User


🧪 Jenkins Job Creation & Execution
🔹 Step 8: Create New Job

Click New Item

Job Name: sample-build

Select Freestyle Project

Click OK


🔹 Step 9: Configure Build Step

Scroll to Build Steps → Add Build Step → Execute Shell

Paste:

echo "Jenkins Build Started"
date
echo "Build Completed Successfully"


Click Save


🔹 Step 10: Run Jenkins Job

Click Build Now

Build number appears under Build History

🔹 Step 11: View Console Output

Click Build #1

Click Console Output

Expected Output:

Jenkins Build Started
<current date and time>
Build Completed Successfully
Finished: SUCCESS

## for Git Repo Connect

Step 2: Create GitHub repository

Repo name: jenkins-demo

Visibility: Public

Step 3: Add sample code (local / Jenkins server)
git clone https://github.com/<username>/jenkins-demo.git
cd jenkins-demo
echo "Hello Jenkins GitHub Integration" > index.html
git add .
git commit -m "Initial commit"
git push origin main

# back to jenkies link
in create project select free style
in scource mnage select GIT
and change Branch Master To main a
save 
and build now 
